# Advanced To-Do List Management System

Full-stack task management app — React + Express + PostgreSQL (Prisma) + JWT Auth.

## Structure
- `client/` — React frontend (Vite)
- `server/` — Express backend (Prisma + PostgreSQL)

## Setup
See `server/.env.example` and `client/.env.example` for required environment variables.